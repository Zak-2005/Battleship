import Ship from "./ship"
import Player from "./Player"
import Gameboard from "./Gameboard"
/*test("computer generates random coordinates", ()=>{
    let player = new Player()
    expect(player.randomCoordinates()).toBeGreaterThanOrEqual([0,0])
})*/
